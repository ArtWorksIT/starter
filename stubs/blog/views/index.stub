{{-- Generated by Starter Package --}}
<x-layout>
    <h1>Blog</h1>

    @if ($posts->count() === 0)
        <p>No posts yet.</p>
    @else
        <ul>
            @foreach ($posts as $post)
                <li>
                    <a href="{{ route('blog.show', $post->slug) }}">
                        {{ $post->title }}
                    </a>
                    <div>
                        @if ($post->excerpt)
                            {{ $post->excerpt }}
                        @endif
                    </div>
                </li>
            @endforeach
        </ul>

        {{ $posts->links() }}
    @endif
</x-layout>
