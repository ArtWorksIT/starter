{{-- Generated by Starter Package --}}
<x-layout>
    <h1>Apply for {{ $job->title }}</h1>

    @if (session('status'))
        <p>{{ session('status') }}</p>
    @endif

    <livewire:careers.apply-job :slug="$job->slug" />
</x-layout>
